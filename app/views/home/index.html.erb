<!DOCTYPE html>
<html lang="en">
<head>
  <title>Review Maiden</title>
  <meta charset="UTF-8"/>
  <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
  <%= stylesheet_link_tag 'bootstrap.min', 'fullcalendar', 'matrix-style', 'matrix-media', 'font-awesome', 'jquery.gritter', 'home' %>
  <link rel="stylesheet" href="/resources/demos/style.css">
  <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

  <link rel="stylesheet" type="text/css" href="../../chart/css/jquery.jqChart.css"/>
  <link rel="stylesheet" type="text/css" href="../../chart/css/jquery.jqRangeSlider.css"/>
  <link rel="stylesheet" type="text/css" href="../../chart/themes/smoothness/jquery-ui-1.10.4.css"/>
  <script src="../../chart/js/jquery-1.11.1.min.js" type="text/javascript"></script>
  <script src="../../chart/js/jquery.mousewheel.js" type="text/javascript"></script>
  <script src="../../chart/js/jquery.jqChart.min.js" type="text/javascript"></script>
  <script src="../../chart/js/jquery.jqRangeSlider.min.js" type="text/javascript"></script>
</head>
<body>
<div id="sidebar">
  <a href="<%= home_index_path %>" class="visible-phone"><i class="icon icon-home"></i> Dashboard</a>
  <ul>
    <li class="active"><a href="<%= home_index_path %>"><i class="icon icon-home"></i><span> Dashboard</span></a></li>
    <li><a href="<%= home_reviews_path %>"><i <i class="icon icon-comments"></i>
      <span>Reviews</span></a></li>
    <li><a href="<%= home_location_listing_path %>"><i class="icon icon-inbox"></i> <span>Locations
        </span></a></li>
    <!-- <li><a href="#"><i class="icon icon-list-alt"></i>
    <span>Reports  </span></a></li> -->
    <% if current_user.has_role?(:Client) || current_user.has_role?(:superadmin) %>
        <li><a href="<%= home_requests_path %>"><i class="icon icon-edit"></i>
          <span>Requests</span></a>
        </li>
    <% end %>
    <li class="signout">
      <a href="<%= home_settings_user_path %>"><i class="icon icon-cog"></i> <span>Settings</span> </a></li>
    <li>
      <a href="<%= destroy_user_session_path %>" method="delete" data="{confirm= 'Are you sure?'}"><i class="icon icon-signout"></i><span>Sign Out</span>
      </a></li>
    <li class="settingsignout">
      <a href="<%= destroy_user_session_path %>" method="delete" data="{confirm= 'Are you sure?'}"><i class="icon icon-signout"></i><span>Sign Out</span>
      </a></li>
  </ul>
</div>
<div id="content">
  <div id="content-header">
  </div>
  <p>

  <h3><%= flash[:success] %></h3></p>
  <p>

  <h3><%= flash[:error] %></h3></p>
  <p>

  <h3><%= flash[:notice] %></h3></p>
  <div class="container-fluid top-boxes">
    <div align="center">
      <div class="row">
        <div class="quick-actions_homepage">
          <div class="panel-body">
            <div class="boxcol-xs-7 col-md-8 index-boxes">

              <a class="btn btn-default btn-lg panelstylefirst" role="button">
                <span class="glyphicon glyphicon-list-alt"></span> <label class="box-font box-color-green">
                <div id="request_sent"><%= @request_count %></div>
              </label>Requests Sent
                <label class="lablesize">(in the last month)</label></a>

              <a class="btn btn-default btn-lg panelstyle" role="button"><span class="glyphicon glyphicon-bookmark"></span><label class="box-font box-color-orange">
                <div id="request"><%= @count %></div>
              </label> Requests Pending
                <label class="lablesize">(for this month)</label></a>

              <a class="btn btn-default btn-lg panelstyle" role="button">
                <span class="glyphicon glyphicon-list-alt"></span> <label class="box-font box-color-green">4.9+ </label>
                Current Rating
                <label class="lablesize">(increased from last month)</label></a>
              <a class="btn btn-default btn-lg" role="button"><span class="glyphicon glyphicon-bookmark"></span><label class="box-font box-color-blue">
                <div id="reviews"><%= reviews %></div>
              </label> Total # of Review
                <label class="lablesize">(Overall)</label></a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="boxes-shadow"></div>
  <div class="container-fluid">
    <div class="row-fluid">
      <div class="span1">
      </div>
      <div class="span4 widged-box-margin" style="margin-left: -4%;">
        <div class="widget-box widged-box-width">
          <div class="widget-title bg_ly" data-toggle="collapse" href="#collapseG2">
            <span class="icon"><i class="icon-chevron-down"></i></span>
            <center class="recent-review-style">Recent Reviews</center>
          </div>
          <div class="widget-content nopadding collapse in recent-rev-collapseG3" id="collapseG3">
          </div>
          <div class="widget-content nopadding collapse in recent-rev-collapseG2" id="collapseG2">
            <div id="less">
              <ul class="recent-posts">
                <!--
                 GOOGLE REVIEWS
                 -->
                <% @google_reviews.take(10).each do |gr| %>
                    <li>
                      <div class="response-border">
                        <div class="user-thumb index-user-thumb">
                          <h3>
                            <div class="image-style"> <%= image_tag "respond.png", :alt => "User", :style => "border-radius: 50%" %></div>
                            <h1 class="image-h1-tag"><%= gr.reviewer_name.split(" ")[0] %></h1></h1>
                            <div class="rating-image-style">
                              <% for star in 1..gr.star_rating.to_i %>
                                  <%= image_tag "star.png", :alt => "User#{star}", :style => " height: 12px;    margin-left: -2px;" %>
                              <% end %>
                            </div>
                            <h1 class="rating-img-h1-tag">On<%= gr.google_review_create_time.strftime(" %b %d") %></h1>
                          </h3>
                        </div>
                        <div class="article-post" style="position: relative;">
                          <span class="user-info"> By: <%= gr.reviewer_name %>
                            / Date: <%= gr.google_review_create_time.strftime("%d-%m-%Y") %>
                            / Time: <%= gr.google_review_create_time.strftime('%H:%M') %> </span>

                          <p class="review-content-width"><%= gr.review_comment %></p>
                        </div>
                        <div class="buttonmargin">
                          <p>
                            <!-- BUTTON -->
                            <% if current_user.has_role?(:Client) %>
                                <button type="button" class="btn btn-default btn-mini borrad dabtn " name='note<%= gr.id %>google'>Add
                                  Note
                                </button>
                            <% end %>
                            <a href="#" class="btn btn-success btn-mini borrad respond-btn-style"/>Respond</a>
                          </p>
                        </div>
                      </div>
                      <!--   Add note DIV
                        -->
                      <div id="note<%= gr.id %>google" class="dadiv" style="display:none;">
                        <% a = gr.id %>
                        <% notes = ResponseNote.where(:google_review_id => a).order("created_at DESC") %>
                        <% if notes.exists? %>
                            <div id="noteless<%= gr.id %>">
                              <div class="up-arrow up-arrow1" style="margin-top: 15px;">
                                <% notes.take(2).each do |r| %>
                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                                <% if notes.length > 2 %>
                                    <button class="btn btn-warning " class="note-lenght" name="notemore<%= gr.id %>" name="noteless<%= gr.id %>" onclick="Google_notes(name,id)">+</button>
                                <% end %>
                              </div>
                            </div>
                            <div id="notemore<%= gr.id %>" style="display:none;">
                              <div class="up-arrow" style="margin-top: 15px;">
                                <% notes.each do |r| %>
                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                              </div>
                            </div>
                            <% if current_user.has_role?(:Client) %>
                                <button name="note<%= gr.id %>googleadd" class="btn btn-success btn-mini borrad btn-info add-note-btn" onclick="hideButton(name)">Add
                                  Note
                                </button>
                            <% end %>
                            </br>
                            <!--   Add note TEXTBOX -->
                        <% else %>
                            <div>
                              <% if current_user.has_role?(:Client) %>
                                  <form action="/home/add_note_for_google_reviews">
                                    <input name="id" value="<%= gr.id %>" type="hidden">
                                    <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                                    <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                                  </form>
                              <% end %>
                            </div>
                        <% end %>
                        <!-- add note form  -->
                      </div>
                      <% if current_user.has_role?(:Client) %>
                          <div id="note<%= gr.id %>googleadd" class="dadiv" style="display:none; ">
                            <form action="/home/add_note_for_google_reviews">
                              <input name="id" value="<%= gr.id %>" type="hidden">
                              <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                              <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                              <br>
                            </form>
                          </div>
                      <% end %>
                <% end %>
                <!-- END OF GOOGLE REVIEWS-->
                <% @responses.take(10).each do |r| %>
                    <li>
                      <div class="response-border">
                        <div class="user-thumb index-user-thumb">
                          <h3>
                            <div class="image-style"> <%= image_tag "respond.png", :alt => "User", :style => "border-radius: 50%" %></div>
                            <h1 class="image-h1-tag">John Doe</h1></h1>
                            <div class="rating-image-style"><%#= image_tag "rating.png" , :alt => "User", :style =>"border-radius: 50%" %></div>
                            <h1 class="rating-img-h1-tag">On <%= r.created_at.strftime("%b %d") %></h1></h3>
                        </div>
                        <div class="article-post" style="position: relative;">
                          <span class="user-info"> By: john Deo / Date: <%= r.created_at.strftime("%d-%m-%Y") %>
                            / Time: <%= r.created_at.strftime('%H:%M') %> </span>

                          <p class="review-content-width"><%= r.feedback %></p>
                        </div>
                        <div class="buttonmargin">
                          <p>
                            <!-- BUTTON -->
                            <button type="button" class="btn btn-default btn-mini borrad dabtn " name="note<%= r.id %>">Add
                              Note
                            </button>
                            <button type="button" class="btn btn-success btn-mini borrad drbtn respond-btn" style="width: 72px;;" name="respond1<%= r.id %>">Respond</button>
                          </p>
                        </div>
                      </div>
                      <!--   Add note DIV
                       -->
                      <div id="note<%= r.id %>" class="dadiv" style="display:none;">
                        <% a = r.id %>
                        <% notes = ResponseNote.where(:response_id => a).order("created_at DESC") %>
                        <% if notes.exists? %>
                            <div id="notelessIndex<%= r.id %>">
                              <div class="up-arrow" style="margin-top: 15px;">
                                <% notes.take(2).each do |r| %>
                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                                <% if notes.length > 2 %>
                                    <button class="btn btn-warning note-lenght" name="notemoreIndex<%= r.id %>" id="notelessIndex<%= r.id %>" onclick="Google_notes(name,id)">+</button>
                                <% end %>
                              </div>
                            </div>
                            <div id="notemoreIndex<%= r.id %>" style="display:none;">
                              <div class="up-arrow" style="margin-top: 15px;">
                                <% notes.each do |r| %>
                                    <div class="user-thumb ser-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                              </div>
                            </div>
                            <% if current_user.has_role?(:Client) %>
                                <button name="note<%= r.id %>Form" class="btn btn-success btn-mini borrad btn-info add-note-btn" onclick="hideButton(name)">Add
                                  Note
                                </button>
                            <% end %>
                            </br>
                            <!--   Add note TEXTBOX -->
                        <% else %>
                            <div>
                              <% if current_user.has_role?(:Client) %>
                                  <form action="/home/add_note_for_reviews">
                                    <input name="id" value="<%= r.id %>" type="hidden">
                                    <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                                    <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                                  </form>
                              <% end %>
                            </div>
                        <% end %>
                        <!-- add note form  -->
                      </div>
                      <% if current_user.has_role?(:Client) %>
                          <div id="note<%= r.id %>Form" class="dadiv" style="display:none; ">
                            <form action="/home/add_note_for_reviews">
                              <input name="id" value="<%= r.id %>" type="hidden">
                              <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                              <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                              <br>
                            </form>
                          </div>
                          <% end %><!--Add note for negative response for view less end-->
                      <!--   Respond DIV -->
                      <div id="respond1<%= r.id %>" class="drdiv" style="display:none; margin-bottom: 10px;">
                        <% a = r.id %>
                        <% responds = ClientResponse.where(:response_id => a).order("created_at DESC") %>
                        <% if responds.exists? %>
                            <div id="respondless<%= r.id %>">
                              <div class="up-arrow  up-arrow4" style="margin-top: 15px;">
                                <% responds.take(2).each do |r| %>
                                    <br>

                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p><%= r.client_reply %></p>
                                      <br/></br><br/></br>
                                    </div>
                                <% end %>
                              </div>
                              <% if responds.length>2 %>
                                  <button class="btn btn-warning respond-lenght" name="respondmore<%= r.id %>" id="respondless<%= r.id %>" onclick="Show_respond(name,id)">+</button>
                              <% end %>
                            </div>
                            <div id="respondmore<%= r.id %>" style="display:none;">
                              <div class="up-arrow  up-arrow4" style="margin-top: 15px;">
                                <% responds.each do |r| %>
                                    <br>

                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p><%= r.client_reply %></p>
                                      <br/></br><br/></br>
                                    </div>
                                <% end %>
                              </div>
                            </div>
                            <% if current_user.has_role?(:Client) %>
                                <button name="respond1<%= r.id %>Form" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="width: auto;" onclick="hideButton(name)">Add
                                  Respond
                                </button>
                            <% end %>
                        <% else %>
                            <!--   RESPOND TEXTBOX -->
                            <div>
                              <% if current_user.has_role?(:Client) %>
                                  <form action="/responses/user_response">
                                    <input name="id" value="<%= r.id %>" type="hidden">
                                    <input type="text" name="client_reply" placeholder="Thanks for your support" class="review-note" required maxlength="1024">
                                    <input type="submit" value="Submit Reply" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                                  </form>
                              <% end %>
                            </div>
                        <% end %>
                      </div>
                      <!-- FORM RESPOND -->
                      <% if current_user.has_role?(:Client) %>
                          <div id="respond1<%= r.id %>Form" class="drdiv" style="display:none; ">
                            <form action="/responses/user_response">
                              <input name="id" value="<%= r.id %>" type="hidden">
                              <input type="text" name="client_reply" placeholder="Thanks for your support" class="review-note" required maxlength="1024">
                              <input type="submit" value="Submit Reply" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                              <br>
                            </form>
                          </div>
                      <% end %>
                      <!-- END -->
                    </li>
                <% end %>
              </ul>
            </div>
            <div id="more" style="display: none">
              <ul class="recent-posts">
                <!-- GOOGLE REVIEWS for View All-->
                <% @google_reviews.each do |gr| %>
                    <li>
                      <div class="response-border">
                        <div class="user-thumb index-user-thumb">
                          <h3>
                            <div class="image-style"> <%= image_tag "respond.png", :alt => "User", :style => "border-radius: 50%" %></div>
                            <h1 class="image-h1-tag"><%= gr.reviewer_name.split(" ")[0] %></h1></h1>
                            <div class="rating-image-style">
                              <% for star in 1..gr.star_rating.to_i %>
                                  <%= image_tag "star.png", :alt => "User#{star}", :style => " height: 12px;    margin-left: -2px;" %>
                              <% end %>
                            </div>
                            <h1 class="rating-img-h1-tag">On<%= gr.google_review_create_time.strftime(" %b %d") %></h1>
                          </h3>
                        </div>
                        <div class="article-post" style="position: relative;">
                          <span class="user-info"> By: <%= gr.reviewer_name %>
                            / Date: <%= gr.google_review_create_time.strftime("%d-%m-%Y") %>
                            / Time: <%= gr.google_review_create_time.strftime('%H:%M') %> </span>

                          <p class="review-content-width"><%= gr.review_comment %> </p>
                        </div>
                        <div class="buttonmargin">
                          <p>
                            <!-- BUTTON -->
                            <% if current_user.has_role?(:Client) %>
                                <button type="button" class="btn btn-default btn-mini borrad dabtn " name='indexnote<%= gr.id %>google'>Add
                                  Note
                                </button>
                            <% end %>
                            <a href="#" class="btn btn-success btn-mini borrad respond-btn-style"/>Respond</a>
                          </p>
                        </div>
                      </div>
                      <!--   Add note DIV
                       -->
                      <div id="indexnote<%= gr.id %>google" class="dadiv" style="display:none;">
                        <% a = gr.id %>
                        <% notes = ResponseNote.where(:google_review_id => a).order("created_at DESC") %>
                        <% if notes.exists? %>
                            <div id="indexnoteless<%= gr.id %>">
                              <div class="up-arrow up-arrow1" style="margin-top: 15px;">
                                <% notes.take(2).each do |r| %>
                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 style="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                                <% if notes.length > 2 %>
                                    <button class="btn btn-warning note-lenght" name="indexnotemore<%= gr.id %>" id="indexnoteless<%= gr.id %>" onclick="Google_notes(name,id)">+</button>
                                <% end %>
                              </div>
                            </div>
                            <div id="indexnotemore<%= gr.id %>" style="display:none;">
                              <div class="up-arrow" style="margin-top: 15px;">
                                <% notes.each do |r| %>
                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                              </div>
                            </div>
                            <% if current_user.has_role?(:Client) %>
                                <button name="indexnote<%= gr.id %>googleadd" class="btn btn-success btn-mini borrad btn-info add-note-btn" onclick="hideButton(name)">Add
                                  Note
                                </button>
                            <% end %>
                            </br>
                            <!--   Add note TEXTBOX -->
                        <% else %>
                            <div>
                              <% if current_user.has_role?(:Client) %>
                                  <form action="/home/add_note_for_google_reviews">
                                    <input name="id" value="<%= gr.id %>" type="hidden">
                                    <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                                    <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                                  </form>
                              <% end %>
                            </div>
                        <% end %>
                        <!-- add note form  -->
                      </div>
                      <div>
                        <% if current_user.has_role?(:Client) %>
                            <div id="indexnote<%= gr.id %>googleadd" class="dadiv" style="display:none; ">
                              <form action="/home/add_note_for_google_reviews">
                                <input name="id" value="<%= gr.id %>" type="hidden">
                                <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                                <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                                <br>
                              </form>
                            </div>
                        <% end %>
                <% end %>
                <!-- END OF GOOGLE REVIEWS and add note for Google Reviews-->

                <!---Start of responses of Negative feedback-->
                <% @responses.each do |r| %>
                    <li>
                      <div class="recent-review-boxes">
                        <div class="user-thumb rec-rev-user-thumb">
                          <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                            <h1 class="user-font">John
                              Doe</h1></h1><%#= image_tag "rating.png" , :alt => "User" , :style => "border-radius: 50%;position: relative;top: -16px;width: 46px;left: 10px;" %>
                            <h1 class="recent-rev-created-at">On <%= r.created_at.strftime("%b %d") %></h1></h3>
                        </div>
                        <div class="article-post" style="position: relative;">
                          <span class="user-info"> By: john Deo / Date: <%= r.created_at.strftime("%d-%m-%Y") %>
                            / Time: <%= r.created_at.strftime('%H:%M') %> </span>

                          <p class="review-content-width"><%= r.feedback %> </p>
                        </div>
                        <div class="buttonmargin">
                          <p>
                            <!-- BUTTON -->
                            <% if current_user.has_role?(:Client) %>
                                <button type="button" class="btn btn-default btn-mini borrad dabtn" name="note1<%= r.id %>">Add
                                  Note
                                </button>
                            <% end %>
                            <button type="button" class="btn btn-success btn-mini borrad drbtn respond-btn" style="width: 72px;" name="respond11<%= r.id %>">Respond</button>
                          </p>
                        </div>
                      </div>
                      <!--   Add note DIV -->
                      <div id="note1<%= r.id %>" class="dadiv" style="display:none;" class="dadiv">
                        <% a = r.id %>
                        <% notes = ResponseNote.where(:response_id => a).order("created_at DESC") %>
                        <% if notes.exists? %>
                            <div id="notelessview<%= r.id %>">
                              <div class="up-arrow up-arrow1" style="margin-top: 15px;">
                                <% notes.take(2).each do |r| %>
                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                              </div>
                              <% if notes.length>2 %>
                                  <button class="btn btn-warning respond-lenght" name="notemoreview<%= r.id %>" id="notelessview<%= r.id %>" onclick="Google_notes(name,id)">+</button>
                              <% end %>
                            </div>
                            <div id="notemoreview<%= r.id %>" style="display:none;">
                              <div class="up-arrow" style="margin-top: 15px;">
                                <% notes.each do |r| %>
                                    <div class="user-thumb user-profile">
                                      <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                        <h1 class="user-font">John Doe</h1></h1>
                                        <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                    </div>
                                    <div class="article-post" style="position: relative;">
                                      <p style="min-height: 85px"><%= r.note_text %></p>
                                    </div>
                                    <br/>
                                <% end %>
                              </div>
                            </div>
                            <% if current_user.has_role?(:Client) %>
                                <button name="note1<%= r.id %>Form" class="btn btn-success btn-mini borrad btn-info add-note-btn" onclick="hideButton(name)">Add
                                  Note
                                </button>
                            <% end %>
                            </br>
                            <!--   Add note TEXTBOX -->
                        <% else %>
                            <div>
                              <% if current_user.has_role?(:Client) %>
                                  <form action="/home/add_note_for_reviews">
                                    <input name="id" value="<%= r.id %>" type="hidden">
                                    <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                                    <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                                  </form>
                              <% end %>
                            </div>
                        <% end %>
                        <!-- add note form  -->
                      </div>
                      <% if current_user.has_role?(:Client) %>

                          <div id="note1<%= r.id %>Form" class="dadiv" style="display:none; ">
                            <form action="/home/add_note_for_reviews">
                              <input name="id" value="<%= r.id %>" type="hidden">
                              <input type="text" name="review_note" placeholder="Enter note for review" class="review-note" required maxlength="1024">
                              <input type="submit" value="Submit Note" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                              <br>
                            </form>
                          </div>
                      <% end %>
                    </li>
                    <!--   Respond DIV -->
                    <div id="respond11<%= r.id %>" class="drdiv" style="display:none; margin-bottom: 10px;">
                      <% a = r.id %>
                      <% responds = ClientResponse.where(:response_id => a).order("created_at DESC") %>
                      <% if responds.exists? %>
                          <div id="respondlessview<%= r.id %>">
                            <div class="up-arrow  up-arrow4" style="margin-top: 15px;">
                              <% responds.take(2).each do |r| %>
                                  <br>

                                  <div class="user-thumb user-profile">
                                    <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                      <h1 class="user-font">John Doe</h1></h1>
                                      <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                  </div>
                                  <div class="article-post" style="position: relative;">
                                    <p><%= r.client_reply %></p>
                                    <br/></br><br/></br>
                                  </div>
                              <% end %>
                            </div>
                            <% if responds.length > 2 %>
                                <button class="btn btn-warning respond-lenght" name="respondmoreview<%= r.id %>" id="respondlessview<%= r.id %>" onclick="Show_respond(name,id)">+</button>
                            <% end %>
                          </div>
                          <div id="respondmoreview<%= r.id %>" style="display:none;">
                            <div class="up-arrow  up-arrow4" style="margin-top: 15px;">
                              <% responds.each do |r| %>
                                  <br>

                                  <div class="user-thumb user-profile">
                                    <h3><%= image_tag "respond.png", :alt => "User", :class => "user-image" %>
                                      <h1 class="user-font">John Doe</h1></h1>
                                      <h1 class="create-time">On <%= r.created_at.strftime("%B %d") %></h1></h3>
                                  </div>
                                  <div class="article-post" style="position: relative;">
                                    <p><%= r.client_reply %></p>
                                    <br/></br><br/></br>
                                  </div>
                              <% end %>
                            </div>
                          </div>
                          <% if current_user.has_role?(:Client) %>
                              <button name="respond11<%= r.id %>Form" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="width: auto;" onclick="hideButton(name)">Add
                                Respond
                              </button>
                          <% end %>
                      <% else %>
                          <!--   RESPOND TEXTBOX -->
                          <div>
                            <% if current_user.has_role?(:Client) %>

                                <form action="/responses/user_response">
                                  <input name="id" value="<%= r.id %>" type="hidden">
                                  <input type="text" name="client_reply" placeholder="Thanks for your support" class="review-note" required maxlength="1024">
                                  <input type="submit" value="Submit Reply" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                                </form>
                            <% end %>
                          </div>
                      <% end %>
                    </div>
                    <!-- FORM RESPOND -->
                    <% if current_user.has_role?(:Client) %>
                        <div id="respond11<%= r.id %>Form" class="drdiv" style="display:none; ">
                          <form action="/responses/user_response">
                            <input name="id" value="<%= r.id %>" type="hidden">
                            <input type="text" name="client_reply" placeholder="Thanks for your support" class="review-note" required maxlength="1024">
                            <input type="submit" value="Submit Reply" class="btn btn-success btn-mini borrad btn-info add-note-btn" style="height: 70%; float:right;">
                            <br>
                          </form>
                        </div>
                    <% end %>
                    <!-- END -->
                <% end %>

                <li>
                  <button class="btn btn-warning btn-mini view-less-btn" id="view">View Less</button>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
      <div class="span2">
      </div>
      <div class="span4" style="margin-left: 1%;">
        <div class="widget-box tot-review-graph" id="graph">
          <div class="widget-title"><span class="icon"> </span>
            <h5>Total # of Reviews</h5>
          </div>
          <div align="center">

            <div id="jqChart" class="jqChart">
            </div>
          </div>
          <div class="widget-content nopadding collapse in" id="collapseG2">
            <div class="panel-body">
              <div class="col-xs-8 col-md-8 totrev-uppper-box" align="center">
                <a href="#" class="btn btn-default btn-lg" role="button">
                  <span class="glyphicon glyphicon-list-alt"></span>
                  <label class="labelstyle totreview-positive-box-font">213 </label>Positive</a>
                <a href="#" class="btn btn-default btn-lg" role="button"><span class="glyphicon glyphicon-bookmark"></span>
                  <label class="labelstyle totreview-negative-box-font">11 </label> Negative</a>
              </div>
            </div>
          </div>

          <div class="panel-body">
            <div class="col-xs-8 col-md-8 totrev-down-box" align="center">
              <a href="#" class="btn btn-default btn-lg" role="button">
                <span class="glyphicon glyphicon-list-alt"></span>
                <label class="labelstyle totreview-joindate-box-font">
                  <div id="reviews1"><%= reviews %></div>
                </label>Since Join Date
              </a>
              <a href="#" class="btn btn-default btn-lg" role="button"><span class="glyphicon glyphicon-bookmark"></span>
                <label class="labelstyle totreview-projected-box-font">788</label>Projected</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="span1">
  </div>
  <div class="row-fluid">
    <div id="footer" class="span12">
    </div>
  </div>
</div>
</div>
<script type="text/javascript">
    $('#location_address_id').on('change', function (event) {
        var selected_resource_id = $(this).val();
        $.ajax({
            type: 'GET',
            url: "<%= home_request_pending_path %>",
            data: {id: selected_resource_id},
            success: function (data) {
                $("#request").empty();
                var count = data["count"];
                $("#request").append('' + count + '');
            }
        });
    });
    $("#client_address_id").change(function () {
        var client_id = this.value;
        console.log("==client id==" + client_id);
        $.ajax({
            url: "<%= home_total_reviews_path %>",
            type: "GET",
            data: {client_id: client_id},
            success: function (data) {
                console.log(data);
                $("#reviews").empty();
                var reviews = data["reviews"];
                $("#reviews").append('' + reviews + '');
            }
        });
    });
    $(window).load(function () {
        $("#client_address_id option[value='1']").text('');
    });
    $("#client_address_id").change(function () {
        var client_id = this.value;
        console.log("==client id==" + client_id);
        $.ajax({
            url: "<%= home_total_reviews_path %>",
            type: "GET",
            data: {client_id: client_id},
            success: function (data) {
                console.log(data);
                $("#reviews1").empty();
                var reviews = data["reviews"];
                $("#reviews1").append('' + reviews + '');
            }
        });
    });
    $(window).load(function () {
        $("#client_address_id option[value='1']").text('');
    });
    $("#view_all").click(function () {
        $("#less").hide();
        $("#more").show();
    });
    $("#view").click(function () {
        $("#less").show();
        $("#more").hide();
    });
    function hideButton(name) {
        $("[name=" + name + "]").hide();
    }
    $(".dabtn").click(function () {
        $(".drdiv").hide();
    });
    $(".drbtn").click(function () {
        $(".dadiv").hide();
    });
    var error_message = '<%=flash[:message].present?%>';
    flash_message = '<%=flash[:message]%>';
    if (error_message == 'true') {
        alert(flash_message.replace(/&#39;/g, ""));
    }
</script>

</body>
</html>
















